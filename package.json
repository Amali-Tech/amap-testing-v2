{
  "name": "amap-testing-v2",
  "version": "1.0.0",
  "description": "Amalitech Assessment Platform",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",

   
    "prepare": "husky || true",
    "lint": "npx eslint --ext .js",
    "lint:fix": "npx eslint --ext .js --fix",
    "format:check": "npx prettier --check cypress",
    "format": "npx prettier --write cypress",
    "cypress run": "cypress run",
    "cypress open": "ypress open"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^20.0.1",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.0"
  },
  "devDependencies": {
    "@commitlint/cli": "^18.6.0",
    "@commitlint/config-conventional": "^18.6.0",
    "cypress": "^13.6.3",
    "eslint": "^8.56.0",
    "husky": "^9.0.10",
    "lint-staged": "^15.2.2",
    "prettier": "^3.2.5"
  }
}
